#include <iostream>

using namespace std;

void PrintNumPattern(int num1, int num2, int rnCnt, int origNum){
   cout << num1 << " ";
   cout << " runCount: " << rnCnt << "\tnum1: " << num1 << " \torigNum/3: " << origNum/3 << endl;
   if(rnCnt == 1){
   }
   else if((num1 < 0) || ((num1 > 0)&&(rnCnt <= origNum/3))){
      PrintNumPattern(num1 + num2, num2, rnCnt - 1, origNum);
   }
   else if((num1 > 0)&&(rnCnt > origNum/3)){
      PrintNumPattern(num1 - num2, num2, rnCnt - 1, origNum);
   }
}

void PrintNumPattern(int num1, int num2, int rnCnt, int origNum);

int main() {
   int firstNum;
   int secondNum;
    
   cin >> firstNum;
   cin >> secondNum;
   int rnCnt = (firstNum*2/secondNum)+2;
   PrintNumPattern(firstNum, secondNum, rnCnt, firstNum);
   
   return 0;
}